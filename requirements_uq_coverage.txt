# Minimal requirements for UQ Coverage Experiment only
# This is a subset of requirements.txt for running only the coverage experiment
#
# Installation:
#   pip install -r requirements_uq_coverage.txt
#
# Usage:
#   python scripts/uq_simulation_analysis.py --experiment coverage --methods DPS DAPS --N 200 --K 100

# Core dependencies
numpy>=1.26.0
torch>=2.0.0
omegaconf>=2.3.0
hydra-core>=1.3.0
pandas>=2.0.0
matplotlib>=3.7.0
tqdm>=4.65.0

